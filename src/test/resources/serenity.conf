webdriver {
  # Define el navegador por defecto
  driver = chrome
  autodownload = true

  capabilities {
    browserName = "chrome"
    acceptInsecureCerts = true

    "goog:chromeOptions" {
      args = [
        "remote-allow-origins=*",
        "start-maximized",
        "incognito",
        "disable-infobars",
        "disable-gpu",
        "disable-default-apps",
        "disable-popup-blocking",
        "disable-dev-shm-usage",
        "no-sandbox"
      ]
    }
  }
}

# Configuraciones adicionales de Serenity para un reporte más limpio
serenity {
    take.screenshots = FOR_FAILURES
    test.root = "org.btg.practual"
    logging = QUIET
    console.colors = true
}

# Gestión de ambientes
environments {
  dev {
    webdriver.base.url = "https://ozonodev.btgpactual.com.co/process/chronos"
    api.endpoint = "https://api-qa.btgpactual.com/v1"
    chronos.url = "https://chronos-qa.btgpactual.com"
  }
  uat {
    webdriver.base.url = "https://dev.btgpactual.com"
    api.endpoint = "https://api-dev.btgpactual.com/v1"
    chronos.url = "https://chronos-dev.btgpactual.com"
  }
}

# Configuración por defecto
webdriver.base.url = "https://chronos.example.com"